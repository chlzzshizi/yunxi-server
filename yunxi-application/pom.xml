<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.yunxi</groupId>
        <artifactId>yunxi-server</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>yunxi-application</artifactId>

    <dependencies>
        <dependency>
            <groupId>com.yunxi</groupId>
            <artifactId>yunxi-common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.yunxi</groupId>
            <artifactId>yunxi-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>com.yunxi</groupId>
            <artifactId>yunxi-infrastructure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <!-- 显式声明 spring-tx：@Transactional 就来自这里。
             以前能编译过是因为 mybatis-spring-boot-starter 顺带传了进来 ——
             靠别人的传递依赖拿到自己直接用的注解，哪天别人换实现就断了 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
        </dependency>
        <!-- 显式声明 spring-security-crypto：两个 AuthAppService 直接用到
             BCryptPasswordEncoder。这块代码原先在 controller 里，依赖挂在
             yunxi-interfaces 上；搬进应用层后就得由应用层自己声明，
             不能再顺着 yunxi-infrastructure 的传递依赖拿 -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
