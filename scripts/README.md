# scripts/ —— 真机接口验收脚本

这些不是单元测试。单元测试跑在 Mockito 里、不碰 Spring 也不碰数据库；**这里的每个脚本都要打真机**：一个跑在 8081 上的后端 + Docker 里的 MySQL 和 Redis。它们验证的是"接起来到底对不对"——SQL 真的写进去了吗、状态真的推进了吗、越权真的被挡了吗。

## 为什么它们现在住在仓库里（而不是某个人的 `C:\tmp`）

2026-09-11 之前，这批脚本住在 `C:\tmp` 下。后果是它们**不在版本管理里**：

- 改了共享行为（比如 2026-09-10 的"后端算价"），没有任何东西提醒你这些脚本会受影响；
- `verify-race.sh` 从 `db11044` 起就是坏的，直到隔了一整轮口径修订才被发现——因为它压根没人跑。

那次踩的坑记在 `docs/bug-record.md` 的 **Bug 21 / Bug 22**。Bug 21 的教训原文就是"改了共享行为要把受影响的**全部**脚本列出来重跑"，但当时只在 5 个脚本里改了 1 个——**结论正确 + 没有执行 = 没修**，于是同一个根因换了张脸又犯一次（Bug 22：`verify-race.sh` 把"准备失败"报成"并发失灵"，把排查方向指反了）。

搬进仓库就是为了让改动能跟代码进同一次提交、能看见 diff、能被 review。

## 前置

```bash
docker compose up -d      # yunxi-mysql(3306) + yunxi-redis(6379)
# 后端已在 8081 起着
```

- **必须用 Git Bash 跑**。PowerShell 里的 `bash` 指向 WSL，而 WSL 里既没有 docker 命令也连不上宿主机的 localhost 代理（实测报 `execvpe(/bin/bash) failed`）。
- **后端必须是"已经跑过全部迁移"的那一版**。Flyway 在应用启动时才执行迁移 —— 换了迁移文件却不重启后端，DB 还停在上一个版本，症状是最难认的那种：`verify-coupons.sh` 会全军覆没在 `Unknown column 'used_time'` 上（B3/C2/F3），看起来像"抵扣逻辑写错了"，其实是后端没重启。跑之前对一眼：

  ```bash
  docker exec yunxi-mysql mysql -uroot -pqwaszx123 yunxi -N --default-character-set=utf8mb4 \
    -e "select group_concat(version order by installed_rank) from flyway_schema_history;"
  ```

  **别用 `select max(version)`**：`flyway_schema_history.version` 是 **VARCHAR**，`max` 走字符串比较，于是 `'9' > '10'` —— 库里有 V10 的时候它会告诉你最大版本是 9。2026-09-13 就是这么被它骗了一下的（当时差点把"V10 已执行"读成"后端没重启"）。要看装了哪些版本，`group_concat` 按 `installed_rank` 排出来才是真相。

  另一个更直接的判据：**新加的端点会不会回答**。`curl -s -X POST localhost:8081/api/orders/1/express -H "Authorization: Bearer x"` 回 `Token 无效或已过期`（401）说明这条路由在；回 404 说明跑的还是旧包。
- 需要 `curl`；脚本用 `docker exec yunxi-mysql mysql` 直连数据库核对"落库真相"。
- 只有 `bench-coupon.sh` 额外要 **JDK 21+**（`scripts/loadgen/LoadGen.java` 用虚拟线程）。它优先认 `JAVA_HOME`，因为 **PATH 上的 `java` 在这台机器上是 1.8**。九个 `verify-*.sh` 都不需要 java。
- 演示账号：`manager/admin123`（店长）、`admin/admin123`（管理员）。

## 怎么跑

**从任何目录都行**，路径一律相对脚本自身：

```bash
bash scripts/verify-orders.sh
```

脚本中途产生的中间文件（替换占位符后的请求体、并发请求的响应）写到 `${TMPDIR:-/tmp}/yunxi-e2e/`，不污染仓库。

## 九个脚本分别证明什么

| 脚本 | 断言数 | 证明什么 | 会写脏数据吗 |
|---|---|---|---|
| `verify-auth.sh` | 29 | 认证域本身：员工/顾客登录的四条分支（密码错、用户名不存在、账号停用、无密码顾客）、注册口径（只要手机号+密码）、**手机号已存在且无密码时走"激活"而不是报错**、框架英文原文不外漏（Bug 23） | 会，但收尾删掉并**还原脚手架** |
| `verify-orders.sh` | 74 | 订单主链路：身份越权（A，含 **A6 无店店长建门店单 → 403 说的真话**，Bug 36）、建单与中文编码（B/G）、参数校验 400 而非 500（C，含 **C2b 数量 -1 → 400**）、支付与状态机 `1→2→3→4→5→7`（D）、分页与归属（E）、跨店不隔离（F）、**顾客在线支付**（H：员工/顾客两种 token 各走各的门、付别人的单 → 403 而单子没被动过、现金 → 400、**持锁并发两个支付 → 恰好一 200 一 409 且钱只收一次**）、**快递单号**（I：仅网单 · 仅 6 态、长度 50 / 空白串 / 缺参 → 400、来源检查排在状态检查之前）、**网单门店可选**（J：不传 `storeId` → 200 且库里是 `NULL`、传个不存在的店 → 400、门店单的 `store_id` 没被这次改动带歪） | **会**：新建若干订单、一个 `mgr2` 测试店长（staff id=99）、一个 `mgrnostore` 无店店长（A6，用管理员的接口建的；id 自增，脚本自建自愈） |
| `verify-pricing-authority.sh` | 24 | 后端算价与事务：请求体塞 `unitPrice:1.00` 不生效；不支持的组合/缺行 → 400 而不是 500；一次建单是一个事务（明细写不进去则订单不留）；管理员改价 403 **且价目表没被动过** | **会**：几条订单 |
| `verify-price.sh` | 26 | 定价域**读**接口：鉴权门槛、分类树、洗涤方式、价目表拼装、设计文档 §4.5 样例（衬衫 `15/35/8`、羽绒服 `0/60/0`） | **不完全是只读**（2026-09-18 更正）：读接口本身只读，但准备段的 `login_or_register` 会建一个顾客 `13900000091` 且**不回收** |
| `verify-price-write.sh` | 29 | 定价**写**接口与精洗派生：叶子校验、`精洗=普洗+20` 自动派生、拒绝手填精洗、羽绒服逃生舱、落库真相、**结束前把数据还原** | 会，但自己还原 |
| `verify-race.sh` | 2 | CAS 并发保护：持锁 3 秒把时间窗拉长，两个 `next` 并发 → **恰好一个 200 + 一个 409**，且状态只推进一格（丢更新被挡住） | 会：一条订单 |
| `verify-coupons.sh` | 39 | 券-订单抵扣（§5.8）：券必须属于该单顾客（别人的券 → 400 **且券没被动过**）；网单/门店单**都能用券**，折后价落 `total_amount`；**使用记录三列**（`used=1` / `used_order_id` 指向本单 / `used_time` 由库写入 / 网单 `used_staff_id` 为 NULL、门店单 = 建单员工）；`pay` 按**折后**金额才算付清；并发用同一张券 → **恰好一个 200 + 一个 409，且库里只有 1 张单**（证 CAS 失败时订单真回滚了）；过期券 → 400；`/api/coupons/mine` 只含未使用 + `expired` 标记 + 员工 401；中文券名断言 **hex 字节**；**发券的闸**（J 段：顾客票 → 401 且库里没建出来 / **管理员票 → 403**（2026-09-19 拍板补）/ 店长发 1.01 折 → 400 / 负折扣 → 同一句 400 —— 对照组是准备段那四张 0.50 的券 + J4b 那张换店长发的，都建得出来） | **会**：每跑一次多发 4 张券 + 1 张对照券（`CPN-E2E-CTRL`），**且不回收**（见下） |
| `verify-stores.sh` | 56 | 门店与顾客：`/api/stores` 只含营业中（**且证明是过滤、不是没建上** —— A3c 会验库里那个店名的字节，字节错了 A3 就是假绿）；`lookup-or-create` 同号两次 `created` true→false、出参无 `password`、老顾客不被改名、只在没名字时补名字、姓名超 20 字 → 400 而非 500；**建单的不可信输入**（C 段）：网单 `storeId=999` / 门店单 `customerId=999` / 网单没配送地址，三条各配一个对照组；个人中心 `/api/customers/me` 看/改自己、**塞 customerId 也改不了别人**、员工 401、管理员 403 | 会，但收尾自己删 |
| `verify-admin.sh` | 118 | 管理员侧接口（§4.2 的"只管人与店"）：**员工 CRUD**（A：建号能登录、库里是 BCrypt 不是明文、重名/超长/非法角色都是 400 而非 500、四种 404）；**停用即失效**（B：先证明那张票能用 → 停用 → 同一张票立刻 401 + Redis 里那个水位线键真的在 + **重新启用不会复活旧票**；四处「重新登录」统一走 `relogin()`，它带 `sleep 1`，绕开"作废水位线的同一秒窗口"，见 Bug 46）；**降级即失效**（C：降级后同一张票 401 而**新票是 403** —— 两句不同才说明死因是"票废了"而不是"角色不够"）；**门店管理**（D：建/改/停业，停业后从顾客选店列表消失、往它下单 400，外加**调岗也作废 token** 和**停业不踢人**两条并排）；**闸门·反方向**（E：店长打 `/api/staff` 六个方法 + 门店三个写接口全 403，**但读门店仍然 200**，且九次尝试之后数据一个字没动）；**改密码成功路径**（E14–E19，2026-09-18 深夜补：先证旧票能用 → 重置 → 新密码能登 / 旧密码不能登 / 那张旧票当场 401 / Redis 水位线键在 / 库里哈希真换了 / 别人的票不受影响）；**闸门·原方向**（F：管理员打订单/定价写/顾客仍是原来那三句 + 改价没生效）；**门店引用校验**（G：`store_id=999` → 400，而 99 号**停业**门店允许挂） | **会**：每轮多两行 `staff` + 一行 `stores`（全 `status=0`），累积型 |

`verify-race.sh` 不走 `check`/`checkNot`，它的 2 条是**手写**的 if/else（同时推进 `PASS`/`FAIL` 计数），
判定仍落在最后一行 `[ $FAIL -eq 0 ]` 上。手写断言全仓只有 5 条：race 2、auth 2（A1b/E1b）、orders 1（H10）。

> **2026-09-18 变薄**：总数 **402 → 376**（调用式 397 → 371，5 条手写没动）。
> 逐脚本删掉 26 条：admin 14 / auth 7 / stores 4 / coupons 1；另外五个**一条没动**。
> 每条删除的点名清单 + "为什么它不算密度"见下面「脚本变薄」一节。
>
> **2026-09-18 深夜 · 补测**：**376 → 384**（只有 `verify-admin.sh` 动，110 → 118）。
> 补的是改密码的**成功路径** —— 九个脚本**全量重跑 384 / 384，九个退出码全 0**（实测）。
> ⚠️ 上面表里 `verify-admin.sh` 那一行写的是 **118**（含补测），而「脚本变薄」那张点名表里
> "现在 = 110" 是**变薄当天**的数 —— 两个数都对，各自属于各自的时点。
>
> **2026-09-18 后半夜 · 补闸**：**384 → 389**（只有 `verify-coupons.sh` 动，31 → 36）。
> 补的是修 Bug 42 之后**发券那两道闸的真机证据**（J 段 5 条：顾客票 401 / 发券被拒后库里
> 没这张券 / 1.01 折 400 / 越界券不落库 / 负折扣同一句 400）。
> 九个脚本**全量重跑 389 / 389，九个退出码全 0**（实测）；每个脚本"自印的通过数"与
> 独立数出的 `[OK]` 标记数**逐个相等**（admin 118 / auth 29 / coupons 36 / orders 69 /
> price-write 29 / price 26 / pricing-authority 24 / race 2 / stores 56）。
> ⚠️ 这 5 条**不是"补覆盖"**，是给新修的闸配的探针：修完之后闸在不在、有没有被人
> 顺手删掉，得有一条会在真机上红的断言看着它。
>
> **2026-09-19 · 时序（断言数一条没变：389 → 389）**：动的是 `verify-admin.sh` 里
> 四处"重新登录"的**写法** —— 全部收进一个 `relogin()` 助手（`sleep 1` → 登录）。
> 起因：CI 第一次真跑到脚本层就红在这个脚本上（**116 / 2**，C4b + D5），
> 而**同一份脚本在本机 118 / 118 一直绿**。死因是"作废水位线的同一秒窗口"
> （`iat` 是秒、水位线是毫秒）—— 窗口只在"作废与那次重新登录落在同一自然秒"时成立，
> 而中间夹着两次 `db()`（`docker exec`）。两个环境**跑的不是同一件事**，
> 所以 **本机那份绿靠的是运气，不是保证**。
> ⚠️ "本机为什么躲过"的机制（`docker exec` 慢 → 多半跨过了秒边界）方向一致，
> 但**概率没有量清** —— 结论停在"差异真实存在"；本条的处理也不依赖那个概率
> （`relogin()` 是从构造上排除窗口，不是调概率）。
> 见 `docs/bug-record.md` **Bug 46**；那个窗口现在由 `probe-watermark-race.sh` 盯着（见下）。
> 改完本机九个脚本**全量重跑 389 / 389、九个退出码全 0**（实测，逐脚本 `[OK]` 数与上表逐个相等）。
>
> **2026-09-19 · 拍板"管理员不发券"**：**389 → 392**（只有 `verify-coupons.sh` 动，36 → 39）。
> 拍板之前那个口径是悬置的（Bug 42 修 A 时判据是 `type == "staff"`，**管理员能发券**）；
> 现在闸在 `JwtInterceptor.checkRoleGate` 的**方向二**（拦管理员那三条的旁边）：
> `POST` 且路径精确等于 `/api/coupons` → 403「管理员不参与发券，请使用店长账号」。
> 新增三条真机断言：**J4** 管理员票发券 → 403；**J4a** 被拒后库里没有这张券；
> **J4b 对照** 同一请求体换店长 → 拿到数字 id（没有这条，J4 在"发券整个塌了"时照样绿）。
> J4b 用**另一个券名**（`CPN-E2E-CTRL`）：`CPN-E2E-ADMIN` 要永远 0 行，J4a 的 `^0$` 重跑才成立。
> 九个脚本**本机全量重跑 392 / 392、九个退出码全 0**（admin 118 / auth 29 / coupons 39 /
> orders 69 / price-write 29 / price 26 / pricing-authority 24 / race 2 / stores 56，
> 自印数与 `[OK]` 数逐个相等）；单测 394 → **395**（interfaces 52 → 53）。
> 反向探针（`/tmp` 副本上把 J4 期望值改错 → `[FAIL] J4` + 退 1）也做了，仓库里的脚本没被改。
> **CI 那一侧也跑了**：`34bddb0` 的 workflow 两个 job 全绿（「单元测试」+「真机验收（九个脚本）」），
> 也就是这 392 条在**全新库 / Linux / 快机器**上同样是绿的（Bug 46 暴露问题的那一侧）。
> ⚠️ 本机的 392 与 CI 的绿是**两次跑**：都记，别把其中一个说成全部。

> **2026-09-19 · CI 先停用（用户拍板：「我先不用 ci，github 不能稳定连接」）。**
> 上面那两条 CI 记录是**截止到现在为止的最后一次** —— 不是 CI 判不了，是这条线连不稳
> （推 GitHub 要走代理，TLS 反复 `unexpected eof`，得靠重试），重跑一次的代价超过了它的收益。
> **判定改回本机**：`for s in scripts/verify-*.sh; do bash "$s"; done` 九连跑 + `./mvnw -o -B test`，
> 两个数字照旧写进本文件，**并且照旧连环境一起说**（Bug 46 的教训）：
> 本机 = Windows / 热库 / 慢机器，CI 那次 = Linux / 全新库 / 快机器。
> `.github/workflows/ci.yml` **留着不动** —— 不是删，是暂时不触发；哪天连得上，它扫的还是这九个脚本。
> 代价明写：**"全新库能不能一次跑通"这一侧暂时没人守。** 脚本的准备段已按 Bug 39/41 改成
> "缺了自己建"，但只在本机这份跑了几十轮的库上验过 —— 见 `docs/bug-record.md` Bug 47 的补记。

> **2026-09-19 · 拍板"数量必须为正整数"**：**392 → 393**（只有 `verify-orders.sh` 动，69 → 70）。
> 补的是 Bug 43 那条挂了一天的边界（`quantity=0` 是"把总额弄成 0"的**另一个输入**）拍板后的真机证据：
> **C2b** 数量 **-1** → 400。**C2 管 0、C2b 管负数** —— 判据写 `<= 0` 还是 `== 0`，
> 只有 C2b 分得开（负数不是 0 的重复：单价恒正，负数量能把别的明细**对冲**掉）。
> 单测 395 → **401**（domain 94 → 98、application 236 → 238，common 12 / interfaces 53 没动）；
> 九个脚本**全量重跑 393 / 393、九个退出码全 0**（admin 118 / auth 29 / coupons 39 /
> **orders 70** / price-write 29 / price 26 / pricing-authority 24 / race 2 / stores 56，
> 自印数与上表逐个相等，实测）。
> 反向探针：/tmp 副本上把 C2b 的请求改成 `quantity:1`（期望的 400 就不来了）→ `[FAIL] C2b` + **退 1**。
> ⚠️ **分工写清楚**：C2b 证的是**最外层那道闸**（400 文案）。过了 controller 之后是应用层还是域层拦的，
> **脚本看不出来** —— 本次新加的那两道只有单测能证。见 `docs/bug-record.md` Bug 43「拍板与第三道闸」。

> **2026-09-19 · Bug 36「说实话，仍然拒绝」**：**393 → 397**（只有 `verify-orders.sh` 动，70 → 74）。
> 补的是**无店店长**（`role=1` + `store_id=NULL`，一条走得通的**正路**）建门店单时那句话的证据：
> **A6** → 403；**A6b** 文案「账号还没有归属门店，无法开单，请联系管理员分配门店」；
> **A6c**（`checkNot`）**不许再出现「重新登录」** —— 那条 401「登录信息已升级」把"你没有门店"
> 说成了"你的票过期了"，而重登一万次也不会带上门店（Bug 20 的形状）；**A6d 对照**：同一张票读
> 价目表 → 200（少了它，"票坏了"和"没门店"在脚本里长得一模一样）。
> ⚠️ **A6 这号的脚手架是拿管理员的接口建的，不是 SQL** —— 那才顺带证明"无店店长是合法可建的"；
> 用 SQL 造一个别处造不出来的状态再断言它被拦，证的是自己写下的前提。第二次跑建号那步是
> 400「用户名已存在」（**预期**，建号自己的断言在 `verify-admin.sh` A6），所以脚本**不看它**，
> 只在票拿不到时才用管理员的重置密码接口自愈；另有一条**准备守卫**断 `store_id` 真是 `NULL`
> （哪天有人给他分了门店，A6 会退化成假红，而真因一个字都看不见 —— Bug 38 的规矩）。
> 单测 401 → **405**（interfaces 53 → 57，新增 `OrderControllerTest` 4 条）；九个脚本
> **本机全量重跑 397 / 397、九个退出码全 0**（admin 118 / auth 29 / coupons 39 / **orders 74** /
> price-write 29 / price 26 / pricing-authority 24 / race 2 / stores 56，自印数与 `[OK]` 数逐个相等）。
> 反向探针三条（**全在 `/tmp` 副本上**，仓库里那份没动）：期望文案换回旧话 → `[FAIL] A6b` + 退 1；
> 准备守卫条件取反 → `[准备失败]` + 退 1；A6c 的否定模式换成响应里真有的片段 → `[FAIL] A6c` + 退 1。
> 环境照旧写明：**本机 / Windows / 热库**（CI 停用中，理由见上）。见 `docs/bug-record.md` Bug 36 补记。

> `verify-orders.sh` 的 69 = 原有 38 + **H/I 两段 27** + **J 段 4**（2026-09-13 新增，当日已跑通）。
> `verify-pricing-authority.sh` 是 24 而不是旧表里的 22（步骤 4 加过两条）。
> **`verify-admin.sh` 的 124 现在是实测的**：2026-09-18 首跑 **119 / 5**，修完脚本复跑 **124 / 124**。
> 首跑那 5 条红**全是脚本自己的错**，生产代码一行没动 —— A2b/A2c/A4e/B1/B6c 拿**店长的票**
> 去打 `/api/staff`，而那条前缀店长本来就进不去（E1 断的正是这个），于是 5 条一致地回
> 403 闸门那句话，看着像"新功能坏了"。**这是假红**，见 `docs/bug-record.md` **Bug 37**：
> 改的是探针（B1/B4 现在同一条 URL、D2/D8 换成顾客票），断言总数和分组一个字没变
> （A36/B13/C9/D32/E20/F8/G6）。
>
> 顺带：写这个数的那天，124 确实只是 `grep -c` 数出来的、还没跑过 —— 当时按 Bug 29 立的规矩
> 标了"尚未跑"。现在它跑过了，数字没变，**但那不是因为当时数得准，是因为数完之后没再改过条数**。
>
> **J 段为什么是 4 条而不是 1 条**：口径改成"网单门店可选"后，最省事的写法是只加 J1（不传门店 → 200）。但 J1 单独存在时，**把校验整个删掉**也能让它通过 —— 而"删掉校验"和"允许不选"在下一条断言上就会分家：传 `storeId=999` 也得照样 200。所以 J3（坏店 → 400）必须和 J1 成对，两条一起才能证明放开的是 `null` 这一种输入，不是"门店这件事不用管了"。反过来少了 J1，J3 的 400 也可能来自"没传门店"而不是"店是坏的"。J4 是**回归哨兵**：这次改的是 `orders.store_id` 的校验，最容易顺手带歪的就是门店单那条仍然必须落在店长自己店上的路径。

## 诊断探针：`probe-watermark-race.sh`（2026-09-19，**不在九连跑里**）

它**故意**不叫 `verify-*.sh`：CI 那一步按 `scripts/verify-*.sh` 扫九连跑，本探针不在其中，
也**不进上面那两张计数表**（392 是九个**验收**脚本的账，2026-09-19 起）。它是**诊断工具**，手跑：

```bash
bash scripts/probe-watermark-race.sh      # 后端要在 8081 上起着
```

它钉的是一个已知缺陷（`docs/bug-record.md` **Bug 46**，源头是 Bug 34 的边界①）：
作废水位线写的是**毫秒**、JWT 的 `iat` 只有**秒**，判据是 `iat < 水位线` ——
于是"作废"与"重新登录"落在**同一个自然秒**里时，刚签发的票会被判成旧的（401，
话术是「账号已被停用或权限已变更，请重新登录」）。

| 段 | 干什么 | 期望 |
|---|---|---|
| 甲 | 对齐到秒边界 → 作废 → **立刻**登录 → 打接口 | **401**（就是缺陷本身）；并从**票上的 `iat`** 和 **Redis 里的水位线**取数，证明两者确实落在同一秒 |
| 乙 | 隔出那一秒再登录 | 200（`relogin()` 助手的做法） |
| 丙 | 两张刚才还好用的票，在新一轮作废后一起打接口 | 都 401 —— 杀的是**签发时刻**，不是某一张票 |

**退出码分三档是故意的**：`0` = 窗口复现（现状）/ `1` = 某条断言不符 /
`2` = **场景没造出来**（机器太慢、对齐失败，此时**不下结论** —— "没复现"和"缺陷没了"是两件事）。
将来有人把水位线改成毫秒精度，**甲段会变绿** —— 那天就是 `relogin()` 里那个 `sleep 1` 可以拿掉的那天。

头注释里记着第一版**判错过**的两个坑：别拿 shell 的 `date +%s` 判"同一秒"
（登录往返几百毫秒，读完秒已经翻过去，会把一次判对了的 401 报成红）；
别在"作废"和"登录"之间夹任何调用（连一次 `docker exec redis-cli get` 都能把窗口吃光）。

副作用：建一个 `staff` 行（`probe_wm<时间戳>`，店长），**不回收** —— 与 `verify-admin.sh` 留行的既有做法一致。

## 压测脚本：`bench-coupon.sh`（2026-09-18）

上面九个脚本证明的是**正确性**，一个都没测过**吞吐** —— 全仓库检索过 `TPS|QPS|吞吐|响应时间`，零命中。而"抢券高并发"是这个项目唯一挂着"高并发"四个字的功能，面试问"多少 TPS"原本答不上来。`bench-coupon.sh` 补的就是这一半。

```bash
bash scripts/bench-coupon.sh                    # 默认 500 并发抢 100 库存
N=200 STOCK=100 bash scripts/bench-coupon.sh    # 改并发和库存
```

> 跑**并发曲线**（比较不同并发下的吞吐）时让 `STOCK = N/2`，把成功率固定成 50%。否则 N=100 时 100% 成功、N=500 时只有 20% 成功，写库次数完全不同，曲线会被成功率带偏而不是被并发数带偏。

| 脚本 | 断言数 | 证明什么 | 会写脏数据吗 |
|---|---|---|---|
| `bench-coupon.sh` | 10 | **不超卖**（成功数恰好 = 库存，多了少了都报错）+ **扣减行为符合设计**（库存键停在 `库存−并发`，验证「减了不回补」那条注释）+ **400 的原因是「抢完」不是「重复抢」**（两者 code 都是 400，计数分不出来，只有看 message 才能分开）+ **去重挡得住**（同一顾客并发 N 次恰好成 1 次）。附带产出 TPS / P50 / P95 / P99 | **会**：每跑一次新发 4 张券 + N 个顾客，累积型 |

### 为什么要自己写压测器，而不是装 wrk / hey

**抢券压测每个请求必须是不同顾客。** 同一个顾客并发发 N 次会全部被 Redis Set（`coupon:grabbed:*`）和数据库唯一键（`uk_coupon_customer`）挡下，测到的是"去重生效"而不是"库存竞争" —— 那是个假的高并发测试，数字再好看也没意义。而 wrk/hey 只会对同一个 URL 反复发同一个请求头，喂不了 N 个不同 token（`hey` 只能配一个 `-H`，还是静态的）。

`scripts/loadgen/LoadGen.java` 是 JDK 21 虚拟线程写的，**零依赖、单文件**（`java LoadGen.java` 直接跑），"一个请求一个线程、一个线程一个 token"写起来和同步代码一样。token 由 bash 侧走真实登录拿（`login_or_register`，同 `verify-coupons.sh`），Java 侧只管同时开火和计时。

### 这个脚本带来的三个新坑

**一、所有轮次必须在同一个 JVM 里跑。** 这条最贵，放第一个。

第一版的 `LoadGen` 用法是 `java LoadGen.java <url> <tokens> [n] [label]`，由 `bench-coupon.sh` **每轮调一次、开一个新 JVM**。跑出来三轮 TPS 是 279 / 305 / 303，当时当成"服务端吞吐很稳定"写进了报告 —— 是错的。

揭穿它的办法是造一个**瞬时响应的对照服务端**（`com.sun.net.httpserver`，20 行，不碰库不碰 Redis），拿 `LoadGen` 打它：**N=1 要 116ms，比打真实应用还慢**。然后用 curl 量同一个真实请求：11ms。再在同一个 JVM 里连跑四轮 N=50：p50 是 **344ms → 32ms → 23ms → 58ms**，第一轮比第三轮慢 15 倍。

三轮数字一样，不是因为服务端稳定，是**三轮一样冷**。那个"预热轮"只暖到了服务端（同一个 Spring 进程），客户端每轮都是刚启动的 JVM —— JIT 没编译、类没加载、连接池是空的。

现在 `LoadGen` 的参数是 `<并发数> <标签> <url> <token文件> ...`，**一个 JVM 顺序跑完所有轮**，共享一个 `HttpClient`（连接池跨轮续上）。`bench-coupon.sh` 因此发 **4 张券**：预热1 / 预热2 / 测量 / 对照。为什么预热两轮不是一轮——实测第二轮还没完全收敛。

**收敛与否从输出里直接看得到**，这是采纳这个设计最实际的理由：

```
预热1 295.71 TPS / p50 1467ms     ← 冷
预热2 845.81 TPS / p50  425ms     ← 已热
测量  872.42 TPS / p50  413ms     ← 与预热2 只差 3%，可以采信
```

**前两轮和测量轮的差距就是"这次数字能不能用"的判据。** 差距大就加预热轮重跑，别把没热透的数字填进文档。

完整过程记在 `docs/bug-record.md` **Bug 31**。教训的核心不是"记得预热"，而是**"这个数字是从哪台机器、哪个进程、第几轮出来的"和数字一样重要** —— 和 Bug 29（往文档里填没跑出来的数字）是同一个病：绿/好看 这个信号和"真的量过"脱钩了。

**二、Java 的 stdout 在这台机器上不是 UTF-8。** 实测 `System.out.println("已抢完")` 打到 Git Bash 里是乱码（Windows 控制台代码页是 GBK）。而压测的输出是要被 bash 拿去断言的，转码一次断言就成了玄学。所以 **`LoadGen.java` 的输出一律是 ASCII**，响应里的中文 message 走 **URL 百分号编码**（编码的是 UTF-8 字节）：

| 中文 | 编码后的字节 | 出处 |
|---|---|---|
| 已抢完 | `%E5%B7%B2%E6%8A%A2%E5%AE%8C` | `CouponAppService.grabCoupon` 库存扣成负数时 |
| 你已经抢过了 | `%E4%BD%A0%E5%B7%B2%E7%BB%8F%E6%8A%A2%E8%BF%87%E4%BA%86` | 同上，Redis Set 命中 / 撞唯一键时 |

A7/A8 就是拿这两串比对的。**注意这和"脚本内可以放心写中文"不冲突**：坏掉的只有 Java 进程那一路 stdout，bash 自己的 `echo`/`printf` 一直是字节安全的。

**三、后端起的时候必须压掉 DEBUG 日志。** `application.yml:50` 是 `logging.level.com.yunxi: debug`，而 MyBatis mapper 的包名就在 `com.yunxi` 下 —— 500 并发时每条 `selectById`/`insert` 都会打一行日志，**不关掉你测的是 logback，不是 Redis**。不改配置文件，走 `-D` 覆盖：

```bash
cd yunxi-interfaces && ../mvnw spring-boot:run \
  -Dspring-boot.run.jvmArguments="-Dlogging.level.com.yunxi=warn"
```

脚本会在跑之前检查 JDK 版本（要 21+，虚拟线程）—— **PATH 上的 `java` 在这台机器上是 1.8**，所以它优先认 `JAVA_HOME`。

### 读数字时必须带上的边界

脚本最后会把这几条打出来，**写进简历/报告时不能省**，省了数字就是吹的：

- **压测端和服务端同机**（12 核）。`LoadGen` 的 JVM 和 Spring Boot、MySQL、Redis 抢同一批核 —— 测出来的是**下限**，不是这套代码的上限。
- **Tomcat 默认 `max-threads=200`、Druid `max-active=20`**（`application.yml` 没配线程池），DEBUG 日志已用 `-D` 压掉。
- **两条启动路径都会加上 `-XX:TieredStopAtLevel=1`**（IntelliJ 的 Spring Boot 运行配置、`spring-boot:run` 的 `optimizedLaunch` 默认值，两条都实测验过；pom 里没配，`MAVEN_OPTS` 是空的）。也就是说**这份文档里的所有 TPS 都是在 C1-only 编译下测出来的** —— 它大概是偏低的，但偏多少没人量过，这是本页所有数字上一个没被消掉的边界。
- **单次采样不能信。** 同一个并发下重复跑，TPS 极差最大到 **±22%**（2026-09-18 实测：N=500、同一个配置跑 6 遍是 851–1058）。**要报就报多次的中位数，别报最好的一次。** 24 次运行里有 1 次离群到 p50 1171ms —— 那是机器在打嗝，不是代码坏了，但它足以让单次采样得出完全错误的结论。
- **这台机器分辨不出 20% 以下的差异。** 实测抖动落在 4.8%–22.3%，**比想测的效应还大**。所以下面那组对照实验里"测不出来"要读成**"这台机器没能力判定"**，不是"两者一样"。
- **预热两轮的数字要一起报告**（脚本已经打出来了）。差距大说明 JIT 还没热透，测量轮不能采信。

### 跑出来的是什么（2026-09-18，24 次运行）

**500 并发抢 100 张券、0 超卖**（两遍）：872 / 777 TPS，p50 413 / 497ms，p99 536 / 617ms。

**并发曲线**（每点 3 遍取中位，`STOCK = N/2` 固定 50% 成功率）：

| 并发 | 10 | 20 | 25 | 50 | 100 | 200 | 300 | 500 |
|---|---|---|---|---|---|---|---|---|
| TPS | 320 | 434 | 525 | 594 | **670** | 649 | 568 | 733 |
| p50 | 19ms | 29ms | 32ms | 53ms | 93ms | 188ms | 350ms | 538ms |
| p99 | 31ms | 46ms | 48ms | 84ms | 146ms | 300ms | 494ms | 671ms |

三条结论：

1. **吞吐在 N≈100 就吃满了**：10→100 翻倍（320→670），100→500 只涨 9%（568–733 全在噪声带内）。加到 5 倍并发，买到的基本是零吞吐。
2. **过了拐点 `p50 ≈ N 毫秒`**：八个点里六个落在 N 的 0.93–1.28 倍。这是"吞吐固定、延迟 = 并发 ÷ 吞吐"的标准形态，不是拟合出来的。
3. **形状是健康的**：没有悬崖、没有雪崩点。24 次运行**全部 10 项断言通过**，包括那次离群（TPS 234 / p50 1171ms）**依然 0 超卖** —— 性能抖动和正确性完全解耦。

### 瓶颈在哪：曲线只能给推断，真跑对照才给结论（2026-09-18）

24 次曲线原本是这样推的：Tomcat `max-threads=200` 对不上（饱和在 N≈100，那时线程数远没到 200）、Druid `max-active=20` 不可能是唯一的墙（N=20 就越过它了，吞吐还在涨）。**但那两步都只是推断** —— 曲线饱和可以有一百种原因，"参数对不上"不等于"参数不是因"。

所以补了一组真对照（N=500，每点 3–6 遍取中位，同一天同一台机器）：

| 配置 | 轮次 | 中位 TPS | 范围 |
|---|---|---|---|
| A 平台线程 / Druid 20（基线） | 3 | 857 | 764–858 |
| B **虚拟线程** / Druid 20 | 4 | 885 | 762–915 |
| C 虚拟线程 / **Druid 50** | 6 | 929 | 851–1058 |

- **Tomcat `max-threads=200` —— 实测排除。** B 组把执行线程换成虚拟线程（`jcmd Thread.print` 验过：`http-nio-8081-exec-*` 归零，只剩 `Acceptor`/`Poller`）——**200 个线程那道闸整个消失了**，吞吐纹丝不动（885 vs 857，两组范围完全重叠）。闸拆了流量不变，它就**不是**墙。这条从推断升级成了因果。
- **Druid `max-active=20` —— 测不出来。** C 组把池子开到 50（`Max_used_connections` 从 21 涨到 51，证明真的开上去了），+5.0%，两组范围重叠。**是"这台机器分辨不出 5%"，不是"两者没差别"** —— 呼应上面那条分辨力边界。虚拟线程那组 p50 从 427ms 降到 325ms，是这轮里唯一一个稳定跑出方向来的信号。
- 剩下最可能是 **12 核本身**（670 TPS 摊下来每请求约 6ms CPU，对 Spring Boot + MyBatis 是合理量级）。**这一条仍然只是推断** —— 它没法用"改个参数再跑一轮"来证伪。

**服务端冷启动是 1.87 倍的变量**（记在 Bug 32）。同一个配置（虚拟线程），重启后第一次跑 **443.88 TPS**，热起来之后四次是 775.06 / 821.61 / 842.68 / 846.88（中位 **832**）。所以**预热协议必须包含服务端，不只是压测器的 JVM** —— 判据仍然是收敛：`预热2 与测量只差 3%` 才算热透，`188 → 337 → 444` 这种还在爬的数列一律不采信。这一条差点让我把"虚拟线程更慢"写进报告（444 vs 记录在案的 649），是同一天重测基线才拦下来的。

**顺带发现一个优化点（只发现，没改）：** `CouponAppService.grabCoupon` 第一步是无条件的 `couponMapper.selectById(couponId)`，**每个请求都走一次**。500 并发 = 500 次 select + 约 250 次 insert ≈ **750 次 DB 往返**，其中约 1/3 是替那 250 个注定拿 400 的请求白查的。券活动是极热的小数据（按 id 查、极少变），缓存它就能砍掉这部分。本轮没动业务代码。

## 一个约定：验收门是"全部"，不是"挑几个"

`verify-orders.sh` 里有句注释值得抄在这里——**改了共享行为（算价、状态机、鉴权口径），下面这几个脚本全都要重跑**。2026-09-10 改后端算价时只重跑了 1 个，剩下 4 个里坏了 2 个都没人知道。

2026-09-12 又加一条：**动了 `JwtInterceptor` 的角色闸门，全都在射程内**——拦截器是所有 `/api/**` 的必经之路，它一改，每个脚本里的鉴权断言都可能翻面。

2026-09-13 再兑现一次：券-订单抵扣改的是 `OrderAppService.createOrder`，而**建单是每个造数据的脚本都要走的门**——`verify-orders` / `verify-price-write` / `verify-pricing-authority` / `verify-race` / `verify-stores` 五个脚本里所有"先建单、再断言"的段落全在射程内。新增的 `verify-coupons.sh` 是**加**了一层验证，不是**替换**那次全量重跑。

同一天晚上这条规矩又生效了一次：`/api/customers` 前缀下新增了顾客自助的 `/me`，闸门的**措辞**跟着从"管理员不参与顾客建档"改成"管理员不参与顾客相关操作"（原来那句话在 `/me` 上会变成一句错话）。只是改了一句提示文案，但 `verify-stores.sh` 的 B9 断言的就是这句话，所以它必须跟着改——**闸门的台词也是被测的**。

2026-09-18 又一次，而且是最重的一次：补管理员接口时给 `checkRoleGate` **加了反方向的一条规则**（"某些前缀只许管理员进"）。按上面 09-12 那条规矩，这一改让**九个脚本全在射程内** —— 拦截器是所有 `/api/**` 的必经之路。同一天还改了 `JwtInterceptor` 的构造签名（多收一个 `StaffTokenRevoker`）和 `StoreController`（新增三个写接口 + `/all`）。`verify-admin.sh` 是**加**的一层验证，不替代那次全量重跑。

那次全量重跑**当天晚上跑了**（21:31–21:34，九个连跑）：**八个全绿**（`admin` 124 / `orders` 69 / `stores` 60 / `auth` 36 / `coupons` 32 / `price` 26 / `pricing-authority` 24 / `race` 一 200 一 409），**只有 `verify-price-write.sh` 红了 1 条**（**28 / 29**）。

红的是 A1，回的是拦截器的「未登录」。**先证对照再断失败**：拿一张真顾客票打同一条 URL，回的是 A1 期望的那句「请使用员工账号操作」—— **控制器没坏、拦截器也没坏**。坏的是脚本的**准备段**：它登的那个顾客 `13900000091` 是 `verify-price.sh` 建的，而通配符顺序里 `verify-price-write` 排**前面**（`-` < `.`），号不在库里时它只 login 就拿到一张**空票**，于是发出去的是 `Authorization: Bearer `（空值）→ 拦截器按"没带这个头"处理。**这是 `docs/bug-record.md` 的 Bug 38**，修法只动准备段（换成 `verify-orders` / `verify-coupons` 里**逐字相同**的 `login_or_register`，再给两张票加一道"空就 `exit 1`"的自检），**断言一条没改**。**改完当场复跑：29 / 29 全绿。**

> 这一跑走的是"register 回 400 该手机号已注册 → 转 login"那条支路（脚手架号还在）。**"号不存在 → register 200 拿票"那条支路是修的时候单独证过的** —— 用一个一次性号码把四个分支全打了一遍，证完就删了。所以两条支路都有实测，不是靠"另一条看起来没问题"。

**这条恰好就是本文件开头那句"九个脚本各自独立"的反面教材。** 脚本之间不互相 `source`，所以 `login_or_register` 在三处各存一份**逐字相同**的拷贝（`md5sum` 对过，一致）—— 要的就是"单跑任何一个都成立"。而 `verify-price-write.sh` 的顾客票**不是自己建的**，它把自己的前提挂在了另一个脚本留下的**残留**上，于是"一直绿"只是因为那份残留平时都在。

**同一个形状还有第二处**（`verify-pricing-authority.sh`，2026-09-18 同日发现、**未爆**、一并修了）：它同样只 `login` 不 register、`CUST_ID` 直接查库不校验 —— 一直绿只是因为 `verify-price.sh` 在通配符顺序里排在它**前面**（`"price." < "pricing"`），那个号连跑时总在。它要是爆起来比上面这条更花：空 id 会把请求体打成**畸形 JSON**，B/C/D/E 全红、报的全是"算价坏了"，而 **F 段还会假绿**（F1 断言"不是 200"、F2/F3 断言"前后计数没变"，建单没发出去时三条都无条件成立）。守卫里 `CUST_ID` 判的是**形状不是有无** —— 本脚本的 `db()` 故意不吞 stderr，MySQL 一挂返回的就是 docker 的报错文本，"非空"但根本不可用。详见 `docs/bug-record.md` 的 Bug 38 末节。**改完复跑 24 / 24 全绿。**

### 第三个坑：`db()` 的字集（2026-09-12）

**每个**访问数据库的助手（`db()` / `dbFile()` / `dbQ()` / 内联的 `docker exec`）现在都带 `--default-character-set=utf8mb4`，**别把这个参数删了**。删掉之后 mysql 命令行会按客户端默认的 latin1 收发，中文两个方向都坏：

- 写 → UTF-8 字节被当 latin1 解读再转存 = 双重编码（实测存进去是 `C3A4C2BAE28098…`）
- 读 → 真 UTF-8 的中文转不成 latin1，整串变成 `?????`

**最阴的是双重编码在「读」的时候会抵消回去**：存的是乱的，`SELECT` 出来却是好的。于是"列表里不含这个店名"这类断言**无条件通过**——库里那个名字本来就写不出正确的那六个字，怎么断言都找不到它。`verify-stores.sh` 的 A3 当时就是这么假绿的。

所以造完中文数据要**断言库里的字节**（`verify-stores.sh` 的准备守卫 + A3c 就是干这个的），不能只看"读出来是对的"。

**2026-09-18 补一条更基础的**：`--default-character-set=utf8mb4` 只管"字节怎么解码"，**管不了"字节有没有被改过"**。`db()` 的 SQL 是**命令行参数**，而 `docker` 是原生 Windows exe —— MSYS2 会按当前代码页（中文系统 = GBK）先把参数转一遍，所以

```bash
db "select id from stores where name='云洗中央门店';"   # ← 查的是 GBK 字节序列
```

**永远查不到**（库里是 UTF-8）。而且它不是报错，是**静默返回空** —— 于是"列表里有没有这家店"这类断言会无条件地说没有，正是上面那种假绿。

规矩：**SQL 里有中文就用 `dbQ`（走 stdin），没有才用 `db`**。`verify-admin.sh` 把两个助手并排放着、名字上就能看出区别，就是为了让这件事在调用点上可见。同理，带中文的 SQL 一律不要写进 `-e "..."`。

> 完整的两类编码坑（还有一类是 MSYS2 把非 ASCII 命令行参数转成 GBK，影响 `curl -d`）写在 `verify-stores.sh` 的文件头，要改脚本先读那段。

### 第四个坑：自愈语句只修了**一半**的中文列（2026-09-13，Bug 30）

上面那条"造完中文数据要断言字节"只说了**验**，没说**修**。补一条同样硬的要求：**修的时候要整行修，验的时候要整行验。**

两个脚本造脚手架门店时都是这个形状：

```sql
insert ignore into stores (id,name,address) values (2,'二号门店','验收用');
update stores set name='二号门店' where id=2;   -- ← 只补了 name
```

insert 里的中文列有**两个**，自愈 update 只写了**一个**。于是 `name` 每次跑都被写回正确字节，`address` 一次都没被碰过 —— 两个门店的地址乱码从 09-12 一直躺到 09-13，是**用户在前端下拉框里肉眼**撞见的，八轮脚本全绿、单测 172 全绿都没喊。

区别不在 SQL 写法，在**谁盯着它**：`name` 有准备守卫和 A3c 两条断言看着，烂了会立刻停；`address` 一条断言都没有。而用这家店的 `verify-orders.sh` F 段连店名都不看（它只看订单），所以"脚本全绿"和"库里有乱码"**同时为真，两件事不冲突**。

所以看着 `insert ignore` + `update` 这种自愈组合时，检查方法是机械的、不需要判断力：**把 insert 里所有的非 ASCII 列列出来，逐个问"谁在盯着它"**。答不出来的那几列，就是正在烂或者即将烂的那几列。

顺着这条查出**三处**，第三处比前两处更根本 —— `verify-auth.sh` 造停用账号时用的是

```bash
docker exec yunxi-mysql mysql -uroot -pqwaszx123 yunxi -e \
  "insert ignore into staff (...) values (98,'mgr_disabled','…','停用店长',1,1,0);"
```

中文直接在 `-e` 的参数里，而且**连 `--default-character-set=utf8mb4` 都没有** —— 两个编码坑一起踩，也没有任何自愈语句，`staff id=98` 的名字从建出来那天就是乱的。那处的问题不在"自愈漏了一列"，而在**写进去的这条路本身会转码**：光加守卫没用，守卫会一直正确地失败。所以先把它改成 `printf | docker exec -i … --default-character-set=utf8mb4`，再补自愈和守卫。

**改这三个脚本时，"正跑一次"是不够的**：自愈语句刚把数据写对，紧接着的守卫读出来当然是对的 —— 这样只能证明"没崩"，证明不了守卫会拦。要**先把那行数据人为弄成乱码、只跑守卫**，看它退出码 1 并指名是哪一列。三个脚本都这么正反各跑了一次（Bug 30 有记录）。

### 表格里的断言数是快照

上面那张表的数字是**最后一次全量跑的结果**，不是自动维护的。改了脚本就得回来手工更新——这不是理想状态，但它至少是一个能被 review 的 diff；不写反而没人知道哪个脚本变过。跑一遍对不上就顺手改了。

## 脚本变薄（2026-09-18）：删了 26 条，每条都点名

**账**：断言总数 **402 → 376**（调用式 397 → 371；5 条手写断言没动）。

**先立公式，再报数字**——这张表被手工更新过太多次，而"数字对不上"的根因每次都一样：

```bash
grep -cE '^check(q|Not)? +"|^verdict +"' scripts/verify-*.sh     # 调用式
# 手写式：不在文件头部助手区里的 PASS 自增，全仓共 5 条（race 2、auth 2、orders 1）
```

> ⚠️ 只写 `^check +"` 会漏两样：**`checkq`**（`verify-price.sh` 用它做小数比较）和那 5 条手写。
> 计划里那个"401 条"就是这么漏出来的——按上面公式，变薄前是 **402**。以后引用总数一律连公式一起写。

| 脚本 | 之前 | 现在 | 删掉的（点名） |
|---|---|---|---|
| `verify-admin.sh` | 124 | **110** | A1f A6c A7c A8 A8b A9b A10c A10d A10e D12b D12e D12f D12g G2b |
| `verify-auth.sh` | 36 | **29** | A3 B4b C3 D2b D3b D5 D5b |
| `verify-stores.sh` | 60 | **56** | B2b B4b B13c D5b |
| `verify-coupons.sh` | 32 | **31** | B4 |
| `verify-orders.sh` | 69 | 69 | 一条没动 |
| `verify-price.sh` | 26 | 26 | 一条没动 |
| `verify-price-write.sh` | 29 | 29 | 一条没动 |
| `verify-pricing-authority.sh` | 24 | 24 | 一条没动 |
| `verify-race.sh` | 2 | 2 | 一条没动 |

（`git diff` 里 `-check`/`-verdict` 开头的行数与左列删除条数逐脚本相同——**清单是量出来的，不是回忆出来的**。）

> **2026-09-18 深夜 · 补测（加日期补记，不改上表）**：总数 **376 → 384**，只有 `verify-admin.sh` 动了（**110 → 118**）。
> 补的是一条**量出来的洞**：`PUT /api/staff/{id}/password` 的**成功路径在本仓脚本里从来没跑通过一次** ——
> E5 碰的是它的 403、原 A10d 碰的是它的 404，"200 之后那一串"没人验过。新增 **E14–E19 共 8 条**，
> 骨架照 B 段（停用即失效）：**先证对照组的票能用 → 重置密码 → 新密码能登 + 旧密码不能登 +
> 那张旧票当场 401 + Redis 水位线键真的在 + 库里哈希真的换了 + 别人的票不受影响**。
> 对照组和 E18 特意用 `/api/orders` 而不是 `/api/staff`：此刻该员工已被降级成店长，
> 在 `/api/staff` 上"票好=403、票废=401"，403 会把前后对照搅浑（Bug 37 的坑）。
>
> 上表左列那个"之前 = 124"是**变薄前一天**的数，右列 110 是**变薄后**的数；本行补的 8 条是**之后**加的，
> 所以 `verify-admin.sh` 现在的真实条数是 **118**，不是 110。**三个数各自属于三个时点，别混着引用。**
>
> **2026-09-18 后半夜 · 补闸（同样是加日期补记，不改上表）**：总数 **384 → 389**，
> `verify-coupons.sh` **31 → 36**（J 段 5 条，给新修的 Bug 42 两道闸配探针）。
> 上表右列那个 31 是**变薄当天**的数，现在的真实条数是 **36** —— 同一个坑又踩了一次，
> 所以这张表的右列**只描述"变薄那一刻"**，要引用"现在是多少"一律看上头的计数表。

### 判据：一条断言该住在哪

| 这个断言需要"世界"吗？ | 住哪 |
|---|---|
| 不需要，只是**输入 → 输出** | **单测** |
| 需要（库 / HTTP / token / 序列化 / 拦截器 / **真实列宽** / SQL 语义），单线程一次请求 | **脚本** |
| 需要，跨两个请求或并发交错 | **脚本**（时序 / 压测） |

### 三条纪律（缺一条就会变成"借着变薄删测试"）

1. **搬走 ≠ 删掉**：每搬走一条，下层必须先有它的家 —— 单测里已有的**直接删**，没有的**先补单测再删**。
2. **每规则留一条代表探针**：证"这条路真接上了"。只搬走**边界密度**——同一属性的第 2、3 份拷贝（20 字/21 字/emoji 这种）。
3. **成对的断言不算密度**：脚本里"回显 + 查库"、"准备守卫 + 结论断言"是**故意**的两条（前者保证信号，后者保证诊断），一条都不能删。

### 逐条点名：删掉的 26 条各自的下家

**`verify-admin.sh` −14**

| 删掉 | 为什么能删 |
|---|---|
| A1f 库里没有明文密码 | A1e 的 `$2a$` 前缀已经蕴含它 |
| A6c 重名被拒后库里还是只有一行、A7c 超长被拒后库里没这个号 | 「create 校验失败一笔都不写」的单测家：`StaffAdminAppServiceTest`（:506 空密码/超 72 字节、:620 姓名空白 → 400 且不写库） |
| A8 `role=5`、A8b `role=null`、A9b 密码为空 | create 与 update 两侧的字段校验在 `StaffAdminAppServiceTest` 里**各有成对一套**；脚本留 A9（必填）、A9c（状态范围）当代表 |
| A10c / A10d / A10e 第 3–5 种 404 | 四条路由里留 A10 + A10b 两条代表；"路径上那个资源不存在 ⇒ 404"这条口径由它们钉住，第三第四条只是换了个动词 |
| D12b 消息里没有英文、D12e 地址为空、D12f 建店地址空、D12g `status=2` | 同上，家是 `StoreAdminAppServiceTest`（:115 名称/地址为空 → 400 一个字不写、:212 status 不是 0/1 → 400 不写库不查库）；脚本留 D12 / D12c / D12d |
| G2b 被拒后门店没被改坏（连带删掉只服务它的 `BEFORE_SID` 取数） | 与 G2 是同一条规则的第二面 |

**`verify-auth.sh` −7**

| 删掉 | 家 |
|---|---|
| A3 用户名不存在、C3 手机号不存在 | "不可枚举"这条性质本来就要**两句话对照**才成立，一条脚本断言只是它的一半：`StaffAuthAppServiceTest.unknownUser` + `wrongPassword` 是成对的两条，各自断言对方那句话；顾客侧是 `CustomerAuthAppServiceTest.badCredentialsAreIndistinguishable` |
| D5、D5b 注册缺 password | `CustomerAuthAppServiceTest.rejectBlankFields` |
| D2b、D3b 第 2、3 份"没有英文" | 这个**属性**归 `GlobalExceptionHandlerTest` 的五条通道；脚本里由 D1c 留一条代表 |
| B4b 库里不是明文密码 | B4 的 `^[$]2a[$]10[$]` 锚已蕴含；而 `CustomerAuthAppServiceTest.hashesPasswordBeforeSaving` 更直接——验 encoder 被调用过，不靠字符串猜 |
| （连带）`PHONE_ABSENT` 变量定义 | 只服务上面两条探针，没有别的用处 |

**`verify-stores.sh` −4**

| 删掉 | 家 / 代表探针 |
|---|---|
| B2b 同一个 `customerId` 的回显 | B2（`created` true→false）+ B3b（只有一行）已经说全；`CustomerAppServiceTest.returnsExisting` |
| B4b 第 3 份"出参里没有 password" | `CustomerAppServiceTest.viewHasNoPasswordField` + `CustomerProfileAppServiceTest`:225；脚本留 B4 那条 |
| B13c 被拒后库里没这个号 | B6b 已经在为同一个手机号断言 `count=0`；"长度校验失败一个字都不写"的家是 `CustomerAppServiceTest.tooLongNameNotFilled`；本脚本由 D5c 在 `/me` 上留一条 |
| D5b 第 2 份"没有英文" | 本脚本的代表是 B13b |

**`verify-coupons.sh` −1**：B4（⊂ H4）；连带删掉只服务它的 `MINE=$(curl …)`。

### 五个脚本一条没动 —— "规则级"不等于"可删"

- **`verify-orders.sh`（69）**：C 段那批（缺 `source`、数量不是正整数、配送地址为空…）正是**计划 §1.3 明确豁免**的"请求体形状/装配"探针——它们证的是 DTO 绑定没吞参数、文案没被全局异常处理器改掉；其余是状态机 / CAS / 并发 / hex 字节。另外 `/final-pay` 这条路由**全仓脚本只在这里被碰过一次**（A5），删了它那条路由就没人守了。
- **`verify-race.sh`（2）**、**`verify-price.sh`（26）**：前者是时序，后者整个是**纯接线**（种子数据 + 读模型拼装 + JSON 字段名），本来就该长这样。
- **`verify-price-write.sh`（29）**、**`verify-pricing-authority.sh`（24）**：B/C–G 段每一条都是**它自己那条规则唯一的探针**（精洗 `=普洗+20` 派生、羽绒服逃生舱、后端算价压过请求体），没有密度可搬。

### 这轮顺带量出来的两个洞（都只记录，不改）

1. **`PUT /api/staff/{id}/password` 的成功路径从没被任何脚本跑通过。** 只有 E5 碰了它的 403、原 A10d 碰了它的 404。200 → 新密码能登、旧密码不能登、旧票当场作废这条链，目前只有应用层单测兜着（`verify-admin.sh` 的 C 段注释里有完整记录）。
2. **`CouponAppServiceTest` 没有"已用的券不再出现在 `/mine`"的显式测试** —— 这条规则现在只有 `verify-coupons.sh` 的 H4 一条断言在守。

### 六份拷贝：`login_or_register`

它在本仓库有**六份**（orders / coupons / price / price-write / pricing-authority / race），彼此**逐字一致**。为什么不 `source` 一份：任何一个脚本都必须能**单独跑通**（`bash scripts/verify-race.sh` 不该因为没先跑 coupons 而挂）。

复核命令（六行必须一模一样）：

```bash
for s in orders coupons price price-write pricing-authority race; do
  awk '/^login_or_register\(\) \{/{f=1} f{print} f&&/^\}$/{exit}' scripts/verify-$s.sh | md5sum
done
# 2026-09-18 实测六行都是 5d383e42ede9…
```

改任何一份都要同步改六份，并重跑上面这条命令。**`verify-admin.sh` 与 `verify-stores.sh` 故意不用它**：它们的顾客号每次现拼（`date +%s`），注册必然成功，"失败就登录"是死分支；真走进去反而会拿到一张**上一轮旧顾客**的票，让"注册这件事本身"不再被任何断言看着。两处都写在脚本里。

### 准备段自证（Bug 38 的扫尾，2026-09-18）

九个脚本的准备段现在同一个形状：

- 失败行一律 `==> [准备失败]`（原先 price / price-write / pricing-authority 三个写的是 `  [致命]`，不方便一把 grep）。
- 准备段末尾**真自检**：票判非空、关键 id 判**形状**（`case "$id" in ''|*[!0-9]*)`）。
  **形状不是有无**——`db()` 把 stderr 丢了，MySQL 一挂它返回空串，而空串拼出来的 `"customerId":,` 会让下游每条断言都指不到真因。
- 印**长度**不印票面值：`${#MGR_T}` 而不是 `${MGR_T:0:20}`。票不进 CI 的日志产物。
- 手机号判形状（恰好 11 位数字）：它们是 `date +%s | tail -c 9` 拼出来的，拼歪了后端回 400「手机号格式不正确」，而下游只会说"注册没拿到 token"。

**两个例外，都是故意的**（理由写在脚本里）：

- **`verify-auth.sh` 不在准备段取票**：它的被测对象就是登录本身，票是 A1/A1b、B1 **测出来的结果**。准备段先登一次等于拿被测的东西给自己当脚手架——登录真坏了会报成"准备失败"，把 A1 该报的那条红顶掉。所以票的断言留在 A1b（非空 + 印长度）。
- **`verify-admin.sh` / `verify-stores.sh` 不用 `login_or_register`**（见上一节）。

## 写脚本时的两条硬要求

1. **准备阶段失败要立刻喊停，不能让它继续跑成断言失败。** 拿不到订单 id、状态没到预期、登录没拿到 token —— 直接打印原始响应并 `exit 1`。否则报出来的错会指向完全无关的地方（Bug 22 就是这么把两个 404 读成"CAS 失灵"的）。
2. **中间的 `404 资源不存在` 出现在"资源一定存在"的用例里，本身就是强信号。** 别把它当成"断言没通过"轻轻放过。

## 已知的脏数据

脚本直连数据库，跑完会在库里留下痕迹：`orders` 表的新订单（含中文备注的样例单）、若干注册的顾客。演示前若要干净数据，`down -v` 重建卷再跑一遍迁移即可。

**故意保留**的脚手架（脚本每次运行都会把它们自愈成正确状态，删了下次跑会自动重建）：

| 数据 | 谁用 |
|---|---|
| `stores` id=2「二号门店」+ `staff` id=99 `mgr2`（store_id=2） | `verify-orders.sh` 的 F 段（跨店不隔离） |
| `stores` id=99「云洗停业测试店」（status=0） | `verify-stores.sh` 的 A3（列表只含营业中）；`verify-admin.sh` 的 D3b（`/all` 里能看到它）和 G3（挂到停业门店是允许的） |
| `staff` id=98 `mgr_disabled`（status=0）、`customers` `13700000002` 「WalkIn」（password=NULL） | `verify-auth.sh` 的停用账号 / 无密码顾客两条分支 |
| `customers` `13900000091`（`PriceReader`） | `verify-price.sh` 的准备段建它、`verify-price-write.sh` 与 `verify-pricing-authority.sh` 的 `login_or_register` 复用同一张票的基础。**三家共用一个号**：谁先跑谁建，后跑的走登录分支 |

**清库时要照着这张表躲**：2026-09-13 清测试数据时 `WalkIn` 不在原表里（原表只列了三处），于是被当成"脚本造的顾客"删掉了 —— 它不是测试数据，是 `verify-auth.sh` 每次跑的必要前置条件。现在它会自愈重建，但这张表本该一开始就写全。

`verify-admin.sh` 也留下痕迹，理由和下面那批"累积型"不同，是**被业务规则逼出来的**：这轮"删"的口径是**软停用**（`status=0`），员工行不硬删 ⇒ 用户名被永久占住 ⇒ 用固定用户名的话第二轮就撞"用户名已存在"。所以它的员工号/门店名带本次的时间戳（`e2e_a<epoch>` / `验收新店<epoch>`），代价是每跑一轮多两行 `staff` + 一行 `stores`（全部停在 `status=0`）。**它不硬删是有意的**：那条断言（重名 → 400）只有在"这个名字已经存在"时才成立。

`verify-coupons.sh` 的脚手架是**累积型**，和前三个不一样：它每跑一次就新发 4 张券（**不改 id、不复用**），跑 N 次库里就有 4N 张叫做 `CPN-E2E-*` / 「开业五折券」的券，顾客A 名下一堆已用掉的抢券记录。之所以不复用固定 id：券的可用性绑定在"这个批次还能不能抢"上，复用就得每次重置 Redis 的库存键和已抢集合，那等于把"准备阶段"变成一段容易写错的 SQL；而每轮新发一张的代价只是几张废券。**要干净数据只有 `down -v`**。

`bench-coupon.sh` 比它更狠：**每跑一次多 N 个顾客和 4 张券**（券叫 `BENCH-*`，默认 N=500）。用的是同一套"每轮新发、不复用 id"的理由，而且这里**非这样不可** —— 库存键在第一轮跑完就停在 `库存−并发` 那个负数上了，复用同一张券就得先把它重置回正数，那正好把 A6 要验的东西手动抹掉。券是 4 张而不是 3 张，因为客户端需要两轮预热（见上面坑一）。顾客的手机号带 epoch 前缀（`139` + 10 位秒 + 3 位序号），所以**重复跑不会撞 `uk_phone`**，脚本可以连着跑。

> 2026-09-18 跑那条并发曲线用了 **24 次运行**，加上之前的几轮，攒下 **5665 个压测顾客 + 113 张 `BENCH-*` 券 + 6297 条抢券记录** —— 目前最大的一坨累积数据。**已于当天清掉，不用 `down -v`。**
>
> ### 清理步骤（2026-09-18 实跑过一遍，三张表要一起删）
>
> ```sql
> -- 只删 customers 会留下一堆指向不存在的人的 grabs
> start transaction;
> delete from coupon_grabs where coupon_id in (select id from coupons where name like 'BENCH-%');
> delete from coupons      where name like 'BENCH-%';
> delete from customers    where phone like '139%' and length(phone) = 16;
> commit;
> ```
>
> `yunxi` 库**没有任何真实外键约束**（`information_schema.key_column_usage` 里 `referenced_table_name` 全空），所以删除顺序不强制；逻辑上按 grabs → coupons → customers 走，是为了中间任何一步断掉都不留悬空引用。
>
> ### 删之前必须跑的四个交叉检查（都应当是 0）
>
> 压测数据是一座**自包含的孤岛** —— 这是它能被安全删掉的全部理由，而这个性质要**当场验**，不能假设：
>
> ```sql
> select '压测顾客身上有订单?', count(*) from orders o join customers cu on cu.id=o.customer_id
>   where cu.phone like '139%' and length(cu.phone)=16
> union all
> select '压测顾客抢过非 BENCH 券?', count(*) from coupon_grabs g join customers cu on cu.id=g.customer_id
>   join coupons c on c.id=g.coupon_id
>   where cu.phone like '139%' and length(cu.phone)=16 and c.name not like 'BENCH-%'
> union all
> select 'BENCH 券被人下过单?', count(*) from coupon_grabs g join coupons c on c.id=g.coupon_id
>   where c.name like 'BENCH-%' and g.used_order_id is not null
> union all
> select 'BENCH 券被非压测顾客抢过?', count(*) from coupon_grabs g join coupons c on c.id=g.coupon_id
>   join customers cu on cu.id=g.customer_id
>   where c.name like 'BENCH-%' and not (cu.phone like '139%' and length(cu.phone)=16);
> ```
>
> ### ⚠️ 判据必须用长度卡死，**不能只写 `like '139%'`**
>
> 压测手机号 = `139` + 10 位秒 + 3 位序号 = **16 字符**。
>
> 库里**真的有 11 位的 139 号段顾客**：`13900000011` / `13900000012`（id 5716 / 5717），是 `verify-coupons.sh` 的**顾客A** —— `orders` 里那几张单、`coupon_grabs` 里那几条非 BENCH 的记录，全挂在它名下。**只写 `like '139%'` 会把它俩一起删掉**；而且删完当轮脚本照样绿（下一轮会重新注册），要等下次跑 `verify-coupons.sh` 的 F 段真去用券时才炸 —— 典型的"删完不报错、隔一轮才响"。
>
> 还有一处同形状的陷阱：脚本守卫里那句探测用的 `13900000000` 只有 **11 字符** —— 万一哪次注册成功了它也会躺在库里，而它不是压测造的。
>
> **两个都靠 `length(phone) = 16` 躲开。** `verify-auth.sh` 要的 `13700000002`（WalkIn，11 位）、`13800138000`（张三）也都不在这个形状里。
>
> ### 删完必须验（不能只看行数掉了）
>
> ```bash
> bash scripts/verify-coupons.sh   # 券域的 32 项：确认顾客A 和 CPN-E2E 那批还在
> bash scripts/verify-auth.sh      # WalkIn 那条分支：确认无密码顾客没被误伤
> ```
>
> 2026-09-18 清理后这两个分别是 **32/32** 和 **36/36**。清理**只要跑这两个** —— 它俩的射程正好覆盖被删数据的两个邻居（顾客A / WalkIn）；其余脚本的脚手架（门店 2/99、员工 98/99）压根不在 139 号段里。

> 补记（2026-09-18 晚）：上面那两个数（**32/32**、**36/36**）是**变薄之前**的记录，本身没跑错。
> 当天晚些时候这两个脚本各删了几条密度断言，总数变成 **31** 和 **29** ——
> 引用旧记录时别把它当成"现在的断言数"。变薄的逐条清单见上面「脚本变薄」一节。
>
> 补记（2026-09-18 后半夜）：`verify-coupons.sh` 又补了 5 条发券闸（J 段），现在是 **36**；
> `verify-auth.sh` 仍是 **29**。**这三个数（32 / 31 / 36）各属于一个时点**，别混着引用 ——
> 要"现在是多少"，看开头那张计数表。

跑压测**不改任何业务代码**，所以上面九个脚本和 `mvn -o test` 的基线不受影响；但按"验收门是全部"的规矩，跑完压测要重跑一次 `verify-coupons.sh`，确认券域没被压测搞脏。
